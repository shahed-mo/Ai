[build]
languages = ["python"]

[phases.setup]
nixpkgs = ["libGL", "gcc", "python312Full"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn app:app --host=0.0.0.0 --port=${PORT}"
