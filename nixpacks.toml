[build]
languages = ["python"]

[phases.setup]
nixpkgs = ["libGL", "gcc", "python312Full", "python312Packages.pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn app:app --host=0.0.0.0 --port=${PORT}"
